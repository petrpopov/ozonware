# Base path that frontend uses for API requests.
# For local dev with Vite proxy keep this as /api.
VITE_API_BASE=/api

# Backend target for Vite dev proxy.
# /api/* on localhost:3001 will be proxied to this address.
VITE_API_TARGET=http://localhost:3000
